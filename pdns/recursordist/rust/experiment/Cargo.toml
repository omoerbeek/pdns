[package]
name = "experiment"
version = "0.1.0"
edition = "2021"

[lib]
name = "rusttest"
crate-type = ["staticlib"]

[dependencies]
cxx = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

[build-dependencies]
cxx-build = "1.0"
